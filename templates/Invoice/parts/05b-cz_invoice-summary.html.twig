{# Invoice summary no vat #}
<table style="padding-top: 20px; page-break-inside: avoid;">
    <tr>
        <td>
            <table>
                {% for vatPercent,vatSum in vat_sum%}
                <tr>
                    <th>{{ vatPercent }} %</th>
                    <td>{{ vatSum|format_currency('CZK', {fraction_digit: 2}, locale='cs') }}</td>
                </tr>
                {% endfor %}
            </table>

            {% if show_discount %}
                <table>
                    <tr>
                        <th style="text-align: right">Celkem k zaplacení:</th>
                        <td style="text-align: right">{{ (sum_total_price_inc_margin_vat/100)|format_currency('CZK', {fraction_digit: 2}, locale='cs' ) }}</td>
                    </tr>
                    <tr>
                        <th style="text-align: right">Sleva celkem:</th>
                        <td style="text-align: right">{{ (total_discount/100)|format_currency('CZK', {fraction_digit: 2}, locale='cs' ) }}</td>
                    </tr>
                    <tr>
                        <th style="text-align: right">Celkem k zaplacení po slevě:</th>
                        <td style="text-align: right">{{ ((sum_total_price_inc_margin_discount_vat)/100)|format_currency('CZK', {fraction_digit: 2}, locale='cs' ) }}</td>
                    </tr>
                </table>
            {% endif %}
        </td>
    </tr>
    <tr>
        <td>
            <table style="background-color: #D9D9D9; font-size: 14pt; margin-bottom: 15px; margin-left: 350px">
                <tr>
                    <td style="width: 171px">Celkem k úhradě:</td>
                    <td style="text-align: right; font-weight: bold; min-width: 200px">{{ ((sum_total_price_inc_margin_discount_vat)/100)|format_currency('CZK', {fraction_digit: 2}, locale='cs') }}</td>
                </tr>
            </table>
        </td>
    </tr>
    <tr>
        <td>
            <table>
                <tr>
                    <td style="text-align: left; width: 440px; font-size: 14pt; color: #999; vertical-align: top">&nbsp;</td>
                    <td>
                        <div style="font-weight: bold; font-size: 8pt; text-align: center">Vystavil, razítko, podpis:
                        </div>
                        <img src="{{ asset('/img/razitko_scan.png') }}" alt="Razitko s podpisem" width="150px"/>
                    </td>
                </tr>
            </table>
        </td>
    </tr>
</table>

</body>
</html>